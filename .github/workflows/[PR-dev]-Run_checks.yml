name: Run Checks on PR to dev branch

on:
  pull_request:
    branches:
      dev

jobs:
  check-for-secrets:
    uses: ./[Reusable]-Check-for-secrets.yml@main

  branch-naming:
    uses: ./[Reusable]-Branch_naming_check_dev.yml@main
    
  sonarqube:
    uses: ./[Reusable]-SonarQube_scan.yml@main

  unit-tests:
    uses: ./[Reusable]-Unit-tests.yml@main
     
       
